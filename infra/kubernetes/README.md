# Kubernetes

Deployment manifests will live here once service runtimes are selected.

## Planned Workloads

- Feed handler deployment.
- Stream processor deployment or Flink operator resources.
- Market data API deployment.
- MCP ops server deployment.
- Dashboard deployment.

Stateful dependencies may use managed services in cloud deployments rather than in-cluster storage.
